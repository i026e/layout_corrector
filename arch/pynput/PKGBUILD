pkgbase=('python-pynput')
pkgname=('python-pynput')
_module='pynput'
pkgver='1.2'
pkgrel=1
pkgdesc="Monitor and control user input devices"
url="https://github.com/moses-palmer/pynput"
depends=('python' 'python-xlib')
makedepends=('python-pip')
license=('LGPL')
arch=('any')

source=()
md5sums=()
#source=("https://pypi.python.org/packages/da/39/5735b951a18aa26d23929522adf8f1475b4f4ee8881419241b1b3e2ec254/pynput-${pkgver}-py2.7.egg")
#md5sums=('858fdfb7cecd51c110de7491c3f014bc')



#build() {
    #cd "${srcdir}/${_module}-${pkgver}"
    #cd "${srcdir}/${_module}"
    #python setup.py build
#}

package() {
    depends+=()
    #cd "${srcdir}/${_module}-${pkgver}"
    PIP_CONFIG_FILE=/dev/null pip install --isolated --root="$pkgdir" --ignore-installed --no-deps pynput
}
